[from-queue-fts]

;----------------------------------------------------------------------
; TEMPLATE_DIALPLAN_START_QUEUE_GRABACION-claro_1402_01
;   Autogenerado 2017-02-23 14:09:14.634860
;----------------------------------------------------------------------

exten => 00776,1,NoOp(cola claro_1402_01)
same => n,Gosub(hangup-fts,llamante_handler,1)
same => n,Set(__MONITOR_FILENAME=/var/spool/asterisk/monitor/q${EXTEN}-${STRFTIME(${EPOCH},,%Y%m%d-%H%M%S)}-${UNIQUEID})
same => n,MixMonitor(${MONITOR_FILENAME}.wav)
same => n,SIPAddHeader(Origin:IN)
same => n,SIPAddHeader(IDCliente:${IDCliente})
same => n,SIPAddHeader(IDCamp:17)
same => n,Queue(claro_1402_01,60,tT)


;----------------------------------------------------------------------
; TEMPLATE_DIALPLAN_START_QUEUE_GRABACION-claro_1502
;   Autogenerado 2017-02-23 14:09:14.635823
;----------------------------------------------------------------------

exten => 00777,1,NoOp(cola claro_1502)
same => n,Gosub(hangup-fts,llamante_handler,1)
same => n,Set(__MONITOR_FILENAME=/var/spool/asterisk/monitor/q${EXTEN}-${STRFTIME(${EPOCH},,%Y%m%d-%H%M%S)}-${UNIQUEID})
same => n,MixMonitor(${MONITOR_FILENAME}.wav)
same => n,SIPAddHeader(Origin:IN)
same => n,SIPAddHeader(IDCliente:${IDCliente})
same => n,SIPAddHeader(IDCamp:18)
same => n,Queue(claro_1502,60,tT)


;----------------------------------------------------------------------
; TEMPLATE_DIALPLAN_START_QUEUE_GRABACION-claro_2102
;   Autogenerado 2017-02-23 14:09:14.636796
;----------------------------------------------------------------------

exten => 00778,1,NoOp(cola claro_2102)
same => n,Gosub(hangup-fts,llamante_handler,1)
same => n,Set(__MONITOR_FILENAME=/var/spool/asterisk/monitor/q${EXTEN}-${STRFTIME(${EPOCH},,%Y%m%d-%H%M%S)}-${UNIQUEID})
same => n,MixMonitor(${MONITOR_FILENAME}.wav)
same => n,SIPAddHeader(Origin:IN)
same => n,SIPAddHeader(IDCliente:${IDCliente})
same => n,SIPAddHeader(IDCamp:19)
same => n,Queue(claro_2102,60,tT)


;----------------------------------------------------------------------
; TEMPLATE_DIALPLAN_START_QUEUE_GRABACION-claro_2302
;   Autogenerado 2017-02-23 14:09:14.637685
;----------------------------------------------------------------------

exten => 00779,1,NoOp(cola claro_2302)
same => n,Gosub(hangup-fts,llamante_handler,1)
same => n,Set(__MONITOR_FILENAME=/var/spool/asterisk/monitor/q${EXTEN}-${STRFTIME(${EPOCH},,%Y%m%d-%H%M%S)}-${UNIQUEID})
same => n,MixMonitor(${MONITOR_FILENAME}.wav)
same => n,SIPAddHeader(Origin:IN)
same => n,SIPAddHeader(IDCliente:${IDCliente})
same => n,SIPAddHeader(IDCamp:20)
same => n,Queue(claro_2302,60,tT)


;----------------------------------------------------------------------
; TEMPLATE_DIALPLAN_START_QUEUE_GRABACION-claro_feb23
;   Autogenerado 2017-02-23 14:09:14.638580
;----------------------------------------------------------------------

exten => 007710,1,NoOp(cola claro_feb23)
same => n,Gosub(hangup-fts,llamante_handler,1)
same => n,Set(__MONITOR_FILENAME=/var/spool/asterisk/monitor/q${EXTEN}-${STRFTIME(${EPOCH},,%Y%m%d-%H%M%S)}-${UNIQUEID})
same => n,MixMonitor(${MONITOR_FILENAME}.wav)
same => n,SIPAddHeader(Origin:IN)
same => n,SIPAddHeader(IDCliente:${IDCliente})
same => n,SIPAddHeader(IDCamp:21)
same => n,Queue(claro_feb23,60,tT)


;----------------------------------------------------------------------
; TEMPLATE_DIALPLAN_START_QUEUE_GRABACION-santander
;   Autogenerado 2017-02-23 14:09:14.639444
;----------------------------------------------------------------------

exten => 00771,1,NoOp(cola santander)
same => n,Gosub(hangup-fts,llamante_handler,1)
same => n,Set(__MONITOR_FILENAME=/var/spool/asterisk/monitor/q${EXTEN}-${STRFTIME(${EPOCH},,%Y%m%d-%H%M%S)}-${UNIQUEID})
same => n,MixMonitor(${MONITOR_FILENAME}.wav)
same => n,SIPAddHeader(Origin:IN)
same => n,SIPAddHeader(IDCliente:${IDCliente})
same => n,SIPAddHeader(IDCamp:2)
same => n,Queue(santander,60,tT)


;----------------------------------------------------------------------
; TEMPLATE_DIALPLAN_START_QUEUE_GRABACION-claro_0702_02
;   Autogenerado 2017-02-23 14:09:14.640343
;----------------------------------------------------------------------

exten => 00774,1,NoOp(cola claro_0702_02)
same => n,Gosub(hangup-fts,llamante_handler,1)
same => n,Set(__MONITOR_FILENAME=/var/spool/asterisk/monitor/q${EXTEN}-${STRFTIME(${EPOCH},,%Y%m%d-%H%M%S)}-${UNIQUEID})
same => n,MixMonitor(${MONITOR_FILENAME}.wav)
same => n,SIPAddHeader(Origin:IN)
same => n,SIPAddHeader(IDCliente:${IDCliente})
same => n,SIPAddHeader(IDCamp:15)
same => n,Queue(claro_0702_02,60,tT)


;----------------------------------------------------------------------
; TEMPLATE_DIALPLAN_START_QUEUE_GRABACION-claro_1402
;   Autogenerado 2017-02-23 14:09:14.641160
;----------------------------------------------------------------------

exten => 00775,1,NoOp(cola claro_1402)
same => n,Gosub(hangup-fts,llamante_handler,1)
same => n,Set(__MONITOR_FILENAME=/var/spool/asterisk/monitor/q${EXTEN}-${STRFTIME(${EPOCH},,%Y%m%d-%H%M%S)}-${UNIQUEID})
same => n,MixMonitor(${MONITOR_FILENAME}.wav)
same => n,SIPAddHeader(Origin:IN)
same => n,SIPAddHeader(IDCliente:${IDCliente})
same => n,SIPAddHeader(IDCamp:16)
same => n,Queue(claro_1402,60,tT)
